FROM node:25-alpine

WORKDIR /app

COPY package.json package-lock.json ./
# `npm ci` for reproducible dev container (lockfile re-tracked v1.2.5).
RUN npm ci --legacy-peer-deps

COPY . .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
