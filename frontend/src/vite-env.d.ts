/// <reference types="vite/client" />

// Compile-time version injected by vite.config.ts (`define.__APP_VERSION__`).
// Read from `frontend/package.json` at build time.
declare const __APP_VERSION__: string
